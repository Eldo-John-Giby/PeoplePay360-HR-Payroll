"""Employee / Department / Job Position / Working Schedule / Contract module.

OWNER: Ameen. This file is a stub so app/main.py can import it (Hour 1).
Replace with real endpoints; keep the `router` name.
"""

from fastapi import APIRouter

router = APIRouter()
"""Attendance + Time Off module.

OWNER: Ambuj. This file is a stub so app bootstraps (Hour 1). Replace with
real endpoints; keep the `router` name.
"""

from fastapi import APIRouter

router = APIRouter()
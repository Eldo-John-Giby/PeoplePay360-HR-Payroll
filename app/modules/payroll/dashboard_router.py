"""Payroll Dashboard / analytics endpoints.

OWNER: Steve. This file is a stub so app bootstraps (Hour 1). Replace with
real aggregation endpoints (department roll-ups, payrun summaries, charts);
keep the `router` name.
"""

from fastapi import APIRouter

router = APIRouter()
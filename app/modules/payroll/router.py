"""Payroll module — salary structures/rules, payruns, payslips.

OWNER: Steve. This file is a stub so app bootstraps (Hour 1). Replace with
real endpoints; keep the `router` name.
"""

from fastapi import APIRouter

router = APIRouter()